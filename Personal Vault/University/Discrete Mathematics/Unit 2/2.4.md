**There are integers c and d such that**
3c + 5d c = 1
c = 2, d = -1


**There is an integer n such that**
2^n -1 is prime
n = 2
3 is prime
therefore, 2^n - 1 is prime for some integer n

**A: there is no largest integer**
1. assume ~A: there is a largest integer
2. let N be the largest integer. Then N >= m ∀m∃.  Consider *m=N+1*, which is an integer because its the sum of 2 integers (closure). Then, N >= N+1, which contradicts that it is the largest integer. Therefore, ~A is false so there is no largest integer